apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-service
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/use-regex: 'true'
spec:
  rules:
    - host: www.find-hotel.online
      http:
        paths:
          - path: /api/?(.*)
            pathType: Prefix
            backend:
              service:
                name: findhotel-srv
                port:
                  number: 3333
          - path: /?(.*)
            pathType: Prefix
            backend:
              service:
                name: findhotel-srv
                port:
                  number: 4200
